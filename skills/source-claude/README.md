# Fuente Claude Code (no instalable)

Skills del original [686f6c61/alfred-dev](https://github.com/686f6c61/alfred-dev)
que dependen de runtime que VS Code no tiene. **El instalador no las copia.**

| Skill | Bloqueo |
|-------|---------|
| `memory` | Tools MCP `mcp__plugin_alfred-dev_alfred-memory__*` |
| `style-direction` | Servidor visual + `visual/scripts/*.py` |
| `sonarqube` | Docker + contenedor `sonarqube-alfred` + flujo `/alfred audit` |

En este port la memoria vive en GitHub Issues + `docs/project/status.md`.
Selina ya escribe `docs/style-direction.md` sin el companion visual.
Se conservan aquí como referencia, no como skills activas.
