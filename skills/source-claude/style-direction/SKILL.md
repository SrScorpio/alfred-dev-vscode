---
name: style-direction
description: "Companion visual de Selina (servidor local + scripts Python). Fuente Claude Code — no instalable en VS Code."
disable-model-invocation: true
---

# Guía del servidor visual de Selina (fuente — no instalable)

Depende de `visual/scripts/*.py` y `start-server.sh` del plugin de Claude Code.
En este port Selina escribe `docs/style-direction.md` sin el companion.

El procedimiento original (galería, selector de familia/tipografía/paleta,
tres variantes, sidecar JSON) está en
[686f6c61/alfred-dev](https://github.com/686f6c61/alfred-dev) `skills/style-direction/`.
No copiar a `~/.copilot/skills/`.
